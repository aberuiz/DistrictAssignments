% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assign.R
\name{AssignToDistricts}
\alias{AssignToDistricts}
\title{Assign geocoded points to district layers}
\usage{
AssignToDistricts(
  geocoded,
  layers,
  removeGEO = TRUE,
  join_type = c("intersects", "within"),
  verbose = TRUE
)
}
\arguments{
\item{geocoded}{Output of \code{\link[=GeocodeMembers]{GeocodeMembers()}} (all rows, with \code{geo_x}/\code{geo_y}
and \code{geocode_status}).}

\item{layers}{A list of prepared sf layers (see \code{\link[=prepare_district_layers]{prepare_district_layers()}}).}

\item{removeGEO}{Drop the geometry column from the result (\code{TRUE} = clean
CSV export).}

\item{join_type}{How a point must relate to a polygon to be assigned.
\code{"intersects"} (default) also matches points lying exactly ON a district
boundary (a boundary point touching two districts resolves to the first,
with a message); \code{"within"} requires the point to be strictly inside, so
boundary points receive \code{NA}.}

\item{verbose}{Print messages about multiply-matched points.}
}
\value{
A data.frame (or sf object if \code{removeGEO = FALSE}) with one row per
input member, in the original order, with the district layers' prefixed
columns joined on. Summary statistics are attached as the
\code{"summary_stats"} attribute; see \code{\link[=print_summary]{print_summary()}}.
}
\description{
Assign already-geocoded points (the output of \code{\link[=GeocodeMembers]{GeocodeMembers()}}) to one or
more PREPARED district layers (the output of \code{\link[=prepare_district_layers]{prepare_district_layers()}}).
This function does not prepare/prefix layers itself, so callers reusing
geocoded points across many layer sets pay preparation only once.
}
\details{
Every input row is carried through: rows without coordinates become empty
points and simply receive \code{NA} district columns, and a geocoded point that
falls outside every polygon of a layer receives \code{NA} for that layer.
Supports any number of layers; overlapping polygons within a layer resolve
to the first match (with a message) so a point is never duplicated into
multiple rows. If an input column has the same name as a layer's prefixed
output column (e.g. a re-uploaded previous export still carrying
\code{Congressional_DISTRICT}), the input copy is renamed with an \verb{_input}
suffix (with a message) so the fresh assignment keeps the expected name.
}
\examples{
\dontrun{
pts    <- GeocodeMembers("members.csv", "Street.Address", "City")
layers <- prepare_district_layers("shapes/congress.shp", "Congressional")
result <- AssignToDistricts(pts, layers)
}
}

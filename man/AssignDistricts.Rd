% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assign.R
\name{AssignDistricts}
\alias{AssignDistricts}
\title{Geocode a member list and assign it to districts in one call}
\usage{
AssignDistricts(
  memberList,
  StreetCol = "Street.Address",
  CityCol = "City",
  districtsList,
  removeGEO = TRUE,
  districtNames = NULL,
  restrictToDistrictArea = FALSE,
  boundaries = NULL,
  join_type = c("intersects", "within"),
  verbose = TRUE
)
}
\arguments{
\item{memberList}{A data.frame, or a path to a \code{.csv} / \code{.xlsx} / \code{.xls}
file, of addresses (Excel files require the suggested \code{readxl} package).}

\item{StreetCol}{Name of the street-address column.}

\item{CityCol}{Name of the city column.}

\item{districtsList}{A single sf layer or file path, a character vector of
file paths, or an (optionally named) list of sf layers and/or file paths
(\code{.shp} / \code{.geojson} / \code{.gpkg}). sf objects pass through; paths are read
from disk.}

\item{removeGEO}{Drop the geometry column from the result (\code{TRUE} = clean
CSV export).}

\item{districtNames}{Optional character vector of names, one per layer,
used to prefix each layer's columns.}

\item{restrictToDistrictArea}{If \code{TRUE}, geocoding is limited to the
(padded) union extent of the district layers. Off by default -- see
\code{\link[=compute_search_extent]{compute_search_extent()}} for why. Ignored if \code{boundaries} is supplied.}

\item{boundaries}{Optional explicit geocoding search extent (a bbox).
Overrides \code{restrictToDistrictArea}. Default \code{NULL} = no restriction.}

\item{join_type}{How a point must relate to a polygon to be assigned.
\code{"intersects"} (default) also matches points lying exactly ON a district
boundary (a boundary point touching two districts resolves to the first,
with a message); \code{"within"} requires the point to be strictly inside, so
boundary points receive \code{NA}.}

\item{verbose}{Print progress and a summary to the console.}
}
\value{
See \code{\link[=AssignToDistricts]{AssignToDistricts()}}.
}
\description{
One-shot convenience wrapper around \code{\link[=prepare_district_layers]{prepare_district_layers()}},
\code{\link[=GeocodeMembers]{GeocodeMembers()}}, and \code{\link[=AssignToDistricts]{AssignToDistricts()}}. For the "geocode once, apply
to many districting files" workflow, call \code{\link[=GeocodeMembers]{GeocodeMembers()}} once and then
\code{\link[=AssignToDistricts]{AssignToDistricts()}} repeatedly instead.
}
\examples{
\dontrun{
result <- AssignDistricts(
  memberList    = "members.csv",
  StreetCol     = "Street.Address",
  CityCol       = "City",
  districtsList = list(
    Congressional = "shapes/congress.shp",
    StateSenate   = "shapes/senate.geojson"
  )
)
}
}

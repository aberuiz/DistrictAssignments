% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layers.R
\name{prepare_district_layers}
\alias{prepare_district_layers}
\title{Prepare district layers for assignment}
\usage{
prepare_district_layers(districtsList, districtNames = NULL)
}
\arguments{
\item{districtsList}{A single sf layer or file path, a character vector of
file paths, or an (optionally named) list of sf layers and/or file paths
(\code{.shp} / \code{.geojson} / \code{.gpkg}). sf objects pass through; paths are read
from disk.}

\item{districtNames}{Optional character vector of names, one per layer,
used to prefix each layer's columns.}
}
\value{
A list of prepared sf layers with validated geometry, a coordinate
reference system, and name-prefixed attribute columns.
}
\description{
Normalize, read, validate and name-prefix one or more district layers so
they are ready to hand to \code{\link[=AssignToDistricts]{AssignToDistricts()}}.
}
\details{
Each layer's attribute columns are prefixed with the layer's name (e.g. a
\code{DISTRICT} column in a layer named \code{Congressional} becomes
\code{Congressional_DISTRICT}) so columns from different layers stay distinct in
the joined output. Names are resolved per layer, in order of precedence:
an explicit \code{districtNames} entry, the name on the \code{districtsList} element,
the file's base name (for path elements), then a positional fallback.
}
\examples{
\dontrun{
layers <- prepare_district_layers(list(
  Congressional = "shapes/congress.shp",
  StateSenate   = "shapes/senate.geojson"
))
}
}

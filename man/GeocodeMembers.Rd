% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geocode.R
\name{GeocodeMembers}
\alias{GeocodeMembers}
\title{Geocode a member list}
\usage{
GeocodeMembers(
  memberList,
  StreetCol,
  CityCol,
  boundaries = NULL,
  censusFallback = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{memberList}{A data.frame, or a path to a \code{.csv} / \code{.xlsx} / \code{.xls}
file, of addresses (Excel files require the suggested \code{readxl} package).}

\item{StreetCol}{Name of the street-address column.}

\item{CityCol}{Name of the city column.}

\item{boundaries}{Optional bbox (see \code{\link[=compute_search_extent]{compute_search_extent()}}) to restrict
geocoding. Default \code{NULL} = unrestricted.}

\item{censusFallback}{If \code{TRUE}, addresses the primary (ArcGIS) geocoder
fails on are retried against the free US Census Bureau geocoder (US
addresses only). Recovered rows get coordinates, status \code{"OK"}, and
\code{geo_source = "Census"}. Default \code{FALSE}.}

\item{verbose}{Print progress to the console.}
}
\value{
The member data.frame with these columns added/replaced:
\describe{
\item{original_row_id}{stable 1..N identifier for mapping results back}
\item{Street.Address, City}{the resolved address/city used for geocoding}
\item{geocode_status}{\code{"OK"}, \code{"Missing address"}, or \code{"No geocode match"}}
\item{geo_x, geo_y}{longitude/latitude (\code{NA} where not geocoded)}
\item{geo_source}{which service located the row: \code{"ArcGIS"}, \code{"Census"}
(fallback), or \code{NA} if not geocoded}
\item{geo_*}{additional geocoder fields (geo_score, geo_match_addr, ...)}
}
}
\description{
Geocode a member list ONCE and return every input row, flagged and (where
possible) coordinated. No rows are dropped. Splitting geocoding from
assignment is what enables the "geocode once, apply to many districting
files" workflow: geocode a member list a single time, then reuse those
points across any number of layer sets via \code{\link[=AssignToDistricts]{AssignToDistricts()}}.
}
\details{
The columns \code{original_row_id}, \code{geocode_status}, and everything prefixed
\code{geo_} are reserved: they are (re)written by this function. If the input
already contains them (e.g. a re-uploaded previous export), they are
replaced with freshly computed values so stale results can never leak into
a new run.
}
\examples{
\dontrun{
pts <- GeocodeMembers("members.csv", "Street.Address", "City")
}
}
